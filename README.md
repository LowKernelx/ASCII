# ASCII Value Checker

This is a simple C++ program that takes a single character input from the user and prints its corresponding ASCII value.

## Example


## How to Compile

```bash
g++ -o ascii_checker main.cpp
./ascii_checker
